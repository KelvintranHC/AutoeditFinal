---
description: "⚡ Write some journal entries."
claude-legacy: .cursor/claude/commands/journal.md
cursor-agent: .cursor/agents/journal-writer.md
---

# Cursor command — `/journal`

## Cursor parity (no Task spawn)

- **Primary persona:** Open and follow `.cursor/agents/journal-writer.md` for this command.
- **Skills / workflows:** `.cursor/skills/`, `.cursor/workflows/` (canonical; optional bundle: `.cursor/claude/`).
- **`/cook`:** This command can run **standalone** or as a **phase** inside a Cook session — see `.cursor/agents/cook.md` and `.cursor/commands/cook.md`.

---

Use the `journal-writer` subagent to explore the memories and recent code changes, and write some journal entries.
