---
description: "⚡ Run tests locally and analyze the summary report."
claude-legacy: .cursor/claude/commands/test.md
cursor-agent: .cursor/agents/tester.md
---

# Cursor command — `/test`

## Cursor parity (no Task spawn)

- **Primary persona:** Open and follow `.cursor/agents/tester.md` for this command.
- **Skills / workflows:** `.cursor/skills/`, `.cursor/workflows/` (canonical; optional bundle: `.cursor/claude/`).
- **`/cook`:** This command can run **standalone** or as a **phase** inside a Cook session — see `.cursor/agents/cook.md` and `.cursor/commands/cook.md`.

---

Use the **tester** persona (`.cursor/agents/tester.md`) to run tests locally and analyze the summary report.

**IMPORTANT**: **Do not** start implementing.
**IMPORTANT:** Analyze the skills catalog and activate the skills that are needed for the task during the process.